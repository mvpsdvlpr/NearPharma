{
  "name": "nearpharma-root",
  "private": true,
  "scripts": {
    "vercel-build": "cd backend && npm ci && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10"
  }
}
